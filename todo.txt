x 1. 可识别数字的符号
x 2. 识别数字0作为十进制数
x 3. 测试enum的问题
  4. 写程序测试类
x 5. 增加Warnning类型检查
  6. 增加数组、变量的声明时初始化，以及函数的声明（可能无法完成）
  7. 增加函数参数类型检测，避免前几个参数乱写，只要后几个参数满足参数个数要求，也能正确运行。
     做法：为Token添加一个args字段用于记录该函数的参数个数，以及类型（或者只记录个数）

  8. 注意expression中的level，某些错误可能是错误的优先级导致的。
  9. 表达式的类型需要注意，不能仅仅由左操作数来确定类型。
  10. 测试printf、exit、malloc等内置函数
  11. 区分字符型和数字型，不能简单地将字符型看做数字型
  12. 支持各种C语言常用函数putchar，getchar，scanf等函数
  13. 解决全局变量定义的问题（只允许常量进行初始化）